/**
 * Geocoding Sync Script
 *
 * Batch geocodes all programs with addresses but no coordinates.
 * Uses Nominatim API with rate limiting (1 req/sec).
 */
export {};
//# sourceMappingURL=sync-geocode.d.ts.map